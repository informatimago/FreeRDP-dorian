#!/bin/bash

KRB5CONFIG=$(which krb5-config)
echo arg setpath nb=$#

echo "voici les arguments de setkerberospath :"
echo "\$#"=$#
echo "\$0"=$0
echo "\$1"=$1
echo "\$2"=$2
echo "\$3"=$3

KRB5PREFIX=$(($KRB5CONFIG --prefix) 2>&1)
KRB5VENDOR=$(($KRB5CONFIG --vendor) 2>&1)
echo KRB5PREFIX dodo=$KRB5PREFIX
echo KRB5VENDOR dodo=$KRB5VENDOR
  
if [ $1 = "Heimdal" ]; then
  echo "$PATH" | grep -qE "[H|h]eimdal"
  if [ $? -ne 0 ]; then
    # Heimdal not set in path(*). Here we are supposed to be in a \case 
    # where MIT is installed and running over Heimdal (unless Heimdal is being 
    # installed in a non-default directory, so no "heimdal" string presents in PATH).
    # We set it by default to the default installation of Heimdal (/usr/heimdal).
    # If you want an other PATH please specify KRB_INSTALLED_DIR. 
    # (*) Note in the particular \case where Heimdal is already running, 
    # the Heimdal PATH is one of the default existing paths.
    if [[ $2 = "" ]]; then
      echo "on est la avant vendor"
      [[ $KRB5VENDOR =~ [H|h]eimdal$ ]] \
        && true \
        || export PATH=/usr/heimdal/bin:$PATH
      echo "/usr/heimdal/bin has been added to PATH"
    else
      echo "avant delete trailing: $2"
      SHAVED_PREFIX_DIR=`echo -e "$2" | sed -e 's/\/$//'`
      echo "apres delete trailing: \$2=$2 ; SHAVED_PREFIX_DIR=$SHAVED_PREFIX_DIR"
      [[ $KRB5VENDOR =~ [H|h]eimdal$ ]] \
        && true \
        || export PATH=$SHAVED_PREFIX_DIR/bin:$PATH
      echo "$SHAVED_PREFIX_DIR/bin has been added to PATH"
    fi
    echo PATH apres vendor=$PATH
  else
    echo "Heimdal already set in path"
    # delete Heimdal in PATH and export PATH with Heimdal 
    # to ensure Heimdal is first Kerberos in PATH
    ORIGIN_PATH=$PATH
    CURRENT_PATH=$PATH
    while true
    do
      echo CURRENT_PATH avant=$CURRENT_PATH
      HEIMDAL_PATH=`echo ${CURRENT_PATH%%:*}`
      echo CURRENT_PATH apres=$CURRENT_PATH
      echo HEIMDAL_PATH=$HEIMDAL_PATH
      CURRENT_PATH=`echo ${CURRENT_PATH#*:}`  
      echo CURRENT ICI=$CURRENT_PATH
      echo "$HEIMDAL_PATH" | grep -qE "[H|h]eimdal"
      if [ $? -eq 0 ]; then
        break
      fi
    done
     
    NEW_PATH=`echo ${ORIGIN_PATH/$HEIMDAL_PATH:/}`
    echo NEW_PATH l.63=$NEW_PATH

    if ! [[ $2 = "" ]]; then
      SHAVED_PREFIX_DIR=`echo -e "$2" | sed -e 's/\/$//'`
      MY_PATH=$SHAVED_PREFIX_DIR/bin:$NEW_PATH
      echo "$SHAVED_PREFIX_DIR has been added to PATH"
      export PATH=$MY_PATH
    else    
      if [[ $KRB5VENDOR =~ [H|h]eimdal$ ]]; then
        echo "Heimdal already configured in PATH" 
        export PATH=$KRB5PREFIX/bin:$NEW_PATH 
      else
        ANSWER=no
        while ! [[ $ANSWER =~ [Y|y] ]] && ! [[ $ANSWER =~ [Y|y]es ]] && ! [[ $ANSWER = "" ]]
        do
          read -p "Please specify prefix directory of your Heimdal installation : " YOURPREFIX
          echo "You entered : $YOURPREFIX. OK ? [Y/n]"
          read ANSWER
        done
        SHAVED_PREFIX_DIR=`echo -e "$YOURPREFIX" | sed -e 's/\/$//'`
        export PATH=$SHAVED_PREFIX_DIR/bin:$NEW_PATH
        echo "$SHAVED_PREFIX_DIR/bin has been added to PATH"
      fi
      echo PATH regex l.83=$PATH
    fi
    echo PATH apres reinit et ajout heimdal=$PATH
  fi
else
  echo "MIT wanted : KRB5CONFIG=$KRB5CONFIG"
  echo "$KRB5CONFIG" | grep -qE "[H|h]eimdal"
  if [ $? -eq 0 ]; then
    # delete Heimdal in PATH
    echo "avant le while"

    ORIGIN_PATH=$PATH
    CURRENT_PATH=$PATH
    while true
    do
      echo CURRENT_PATH avant=$CURRENT_PATH
      HEIMDAL_PATH=`echo ${CURRENT_PATH%%:*}`
      echo CURRENT_PATH apres=$CURRENT_PATH
      echo HEIMDAL_PATH=$HEIMDAL_PATH
      CURRENT_PATH=`echo ${CURRENT_PATH#*:}`  
      echo CURRENT ICI=$CURRENT_PATH
      echo "$HEIMDAL_PATH" | grep -qE "[H|h]eimdal"
      if [ $? -eq 0 ]; then
        break
      fi
    done
     
    NEW_PATH=`echo ${ORIGIN_PATH/$HEIMDAL_PATH:/}`
    echo NEW_PATH=$NEW_PATH

    if [[ $3 = "" ]]; then
      export PATH=$NEW_PATH
    else
      SHAVED_PREFIX_DIR=`echo -e "$3" | sed -e 's/\/$//'`
      export PATH=$SHAVED_PREFIX_DIR/bin:$PATH
      echo "$SHAVED_PREFIX_DIR/bin has been added to PATH" 
    fi

    echo PATH apres sup heimdal=$PATH
  else
     if [[ $KRB5VENDOR =~ [H|h]eimdal$ ]]; then
        if [[ $3 = "" ]]; then
          echo "avant until : \$3=$3"
          ANSWER=no
          while ! [[ $ANSWER =~ [Y|y] ]] && ! [[ $ANSWER =~ [Y|y]es ]] && ! [[ $ANSWER = "" ]]
          do
            read -p "Please specify prefix directory of your MIT installation : " YOURPREFIX
            echo "You entered : $YOURPREFIX. OK ? [Y/n]"
            read ANSWER
          done
          SHAVED_PREFIX_DIR=`echo -e "$YOURPREFIX" | sed -e 's/\/$//'`
          export PATH=$SHAVED_PREFIX_DIR/bin:$PATH
          echo "$SHAVED_PREFIX_DIR/bin has been added to PATH"
        else
          SHAVED_PREFIX_DIR=`echo -e "$3" | sed -e 's/\/$//'`
          export PATH=$SHAVED_PREFIX_DIR/bin:$PATH
          echo "$SHAVED_PREFIX_DIR/bin has been added to PATH"
        fi
     else
       echo "PATH is not changed : MIT is already being configured as Kerberos flavour"
     fi
  fi
fi
